import { UploadDetailsType } from "./UploadDetailsTypes"

export type InitialStates = {
    items: any,
    editData: any,
    editMode: boolean,
    showDialog: boolean,
    uploadDetails: UploadDetailsType,
}